import { mkdir, readFile, writeFile } from "node:fs/promises";
import { lessons, sources } from "../curriculum/deep-learning.mjs";

export const escapeHTML = value => String(value).replace(/[&<>"']/g, c => ({"&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;", "'":"&#39;"}[c]));
export function markdown(text) {
  const inline = value => escapeHTML(value).replace(/`([^`]+)`/g, '<code>$1</code>').replace(/\*\*([^*]+)\*\*/g, '<strong>$1</strong>');
  return text.split(/\n\n/).map(p => `<p>${inline(p)}</p>`).join("\n");
}
const version = JSON.parse(await readFile(new URL("../package.json", import.meta.url))).version.split(".").slice(0, 2).join(".");
const output = new URL("../room310files/", import.meta.url);
const header = `<header class="header solid-header"><a class="logo" href="index.html">ROOM<span>310</span></a><nav aria-label="Main navigation"><a aria-current="page" href="study.html">Study</a><a href="games.html">Games</a><a href="/graphs">Graphs</a><a href="index.html#archive">Archive</a><a href="extended-archives.html">Extended Archives</a></nav></header>`;
const shell = (title, description, classes, content, extra = "", attributes = "") => `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="description" content="${escapeHTML(description)}"><title>${escapeHTML(title)} · Room310</title><link rel="stylesheet" href="style.css"><link rel="stylesheet" href="deep-learning.css?v=${version}"><link rel="icon" type="image/png" href="favicon.png"></head>
<body class="${classes}" ${attributes}>${header}${content}<script src="site-polish.js?v=${version}"></script>${extra}</body></html>\n`;
const colab = slug => `https://colab.research.google.com/github/Jakeyp002/room310test/blob/main/room310files/notebooks/${slug}.ipynb`;
const actions = lesson => `<div class="dl-actions"><a class="dl-button dl-primary" data-colab-link href="${colab(lesson.slug)}">Open in Colab ↗</a><a class="dl-button" href="notebooks/${lesson.slug}.ipynb" download>Download notebook</a><a class="dl-button" href="downloads/${lesson.slug}.py" download>Download Python file</a></div>`;
const references = keys => `<ul class="dl-references">${keys.map(key => `<li><a href="${sources[key][1]}">${escapeHTML(sources[key][0])} ↗</a></li>`).join("")}</ul>`;

await mkdir(new URL("downloads/", output), { recursive: true });
await mkdir(new URL("../.runtime/deep-learning/", import.meta.url), { recursive: true });
await writeFile(new URL("../.runtime/deep-learning/notebook-inputs.json", import.meta.url), JSON.stringify({lessons, sources}, null, 2));

const cards = lessons.map((lesson, i) => `<a class="dl-lesson-card" href="${lesson.slug}.html"><div class="dl-card-meta"><span>0${i + 1}</span><span>${lesson.time}</span></div><h3>${escapeHTML(lesson.title)}</h3><p>${escapeHTML(lesson.description)}</p><div class="dl-card-footer"><span>${lesson.runtime === "browser" ? "Run on Room310" : "PyTorch · Colab / local"}</span><strong>Start lesson →</strong></div></a>`).join("\n");
const overview = `<main class="study-page dl-index">
<section class="dl-hero"><div><a class="dl-back" href="study.html">← All courses</a><p class="dl-eyebrow">After Python / Deep learning foundations</p><h1>Small network.<br>Big first step.</h1><p class="dl-lead">Build a neural network you can actually explain. Start with a few numbers, teach them to learn, then bring it all together in PyTorch.</p><div class="dl-actions"><a class="dl-button dl-primary" href="${lessons[0].slug}.html">Start with one neuron →</a><a class="dl-button" href="#curriculum">Explore the 6 lessons ↓</a></div></div>
<figure class="dl-network"><div class="dl-network-label">Your first network / 33 parameters</div><svg viewBox="0 0 420 280" role="img" aria-labelledby="network-title network-desc"><title id="network-title">Two inputs, eight hidden neurons, one output</title><desc id="network-desc">Each input connects to every hidden neuron. The eight hidden neurons connect to one output. Tanh is the hidden activation.</desc><g stroke="#777b67" stroke-width="1">${[95,185].flatMap(y => Array.from({length:8}, (_,i) => `<path d="M65 ${y} L210 ${28 + i * 32} L355 140"/>`)).join("")}</g><g fill="#e2f68a" stroke="#e2f68a">${[95,185].map(y=>`<circle cx="65" cy="${y}" r="14"/>`).join("")}${Array.from({length:8},(_,i)=>`<circle cx="210" cy="${28+i*32}" r="10"/>`).join("")}<circle cx="355" cy="140" r="18" fill="#ffad83" stroke="#ffad83"/></g></svg><figcaption><span>2 inputs</span><span>8 hidden · tanh</span><span>1 output</span></figcaption></figure></section>
<div class="dl-facts"><span><strong>6</strong> guided lessons</span><span><strong>12</strong> practice tasks</span><span><strong>CPU</strong> is enough</span><span><strong>Python</strong> is your prerequisite</span></div>
<section class="dl-intro-grid"><div><p class="dl-eyebrow">Who this is for</p><h2>You know Python.<br>Start learning models.</h2><p>Comfortable with variables, lists, loops, and functions? You are ready. We introduce PyTorch and explain the little bit of slope-and-gradient math as we use it. No advanced Python, GPU, or previous deep-learning course is required.</p><a href="python-study.html">Need a Python refresher? →</a></div><aside class="dl-note"><h2>Read here. Run with confidence.</h2><p>Lessons 1–2 use Room310's interactive Python cells. Lessons 3–6 need real PyTorch: open the included notebook in Colab or run it in your own Python environment.</p><p>The ordinary Room310 compiler does not include PyTorch. Every lesson includes a notebook and Python download; every PyTorch notebook starts with its own setup.</p><a href="${lessons[2].slug}.html#step-1">See the setup guide →</a></aside></section>
<section id="curriculum" class="dl-curriculum"><div class="dl-section-heading"><div><p class="dl-eyebrow">The learning path</p><h2>From one neuron to a working model.</h2></div><p>Follow the order. Predict an output, run the code, change one thing, and explain what happened.</p></div><div class="dl-lesson-grid">${cards}</div></section>
<section class="dl-finish"><p class="dl-eyebrow">What you will leave with</p><h2>A small, complete PyTorch project.</h2><p>A two-input classifier, a training loop you understand, an honest test result, and a saved model you can reload. No chatbot-scale training or expensive hardware.</p><a class="dl-button dl-primary" href="${lessons[0].slug}.html">Begin lesson 01 →</a></section>
<section class="dl-credit"><h2>Build-first, not black-box.</h2><p>This original Room310 course takes inspiration from the bottom-up, code-first progression in Andrej Karpathy's Neural Networks: Zero to Hero. It is not his course, a transcript, or an affiliated program. Our smaller finish line is a simple classifier; his course is an optional next step.</p>${references(["inspiration", "tensors", "networks"])}</section>
</main>`;
await writeFile(new URL("deep-learning-study.html", output), shell("Learn Deep Learning with PyTorch", "A six-lesson introduction to neural networks for Python learners. Build, train, test, and save a small PyTorch model.", "study-index-page deep-learning-study-page", overview));

for (const [i, lesson] of lessons.entries()) {
  const isTorch = lesson.runtime === "pytorch";
  const codeSections = lesson.sections.filter(s=>s.code);
  const fullSource = `# Room310 · ${lesson.title}\n# Original teaching examples. Run top to bottom.\n${isTorch ? "# Requires PyTorch; CPU is enough.\n" : ""}\n` + codeSections.map(s => `# ${s.title}\n${s.code}\n`).join("\n");
  await writeFile(new URL(`downloads/${lesson.slug}.py`, output), fullSource);
  const steps = lesson.sections.map((section, n) => `<section class="dl-step" id="step-${n+1}"><p class="dl-eyebrow">Step ${n+1}</p><h2>${escapeHTML(section.title)}</h2>${markdown(section.text)}${section.code ? `<div class="code-panel authored-code-panel dl-code" ${isTorch ? 'data-static-example' : `data-python-source="${escapeHTML(section.code)}"`}><div class="dl-code-bar"><span>${isTorch ? "PyTorch · run in your notebook" : "Python · try it here"}</span>${isTorch ? `<button type="button" class="dl-copy" aria-label="Copy code for step ${n+1}" hidden>Copy code</button>` : ""}</div><pre tabindex="0" aria-label="Code for step ${n+1}"><code class="language-python">${escapeHTML(section.code)}</code></pre></div>` : ""}${section.check ? `<aside class="dl-check"><strong>Check your result</strong><p>${escapeHTML(section.check)}</p></aside>` : ""}</section>`).join("\n");
  const assignments = `<section class="assignment-section dl-assignments" id="assignments"><p class="dl-eyebrow">Your turn</p><h2>Practice & explain</h2><p>${isTorch ? "Use the practice section at the end of this lesson's Colab notebook, or work in your local PyTorch environment. Keep a copy of your changes." : "Use the assignment workspace or edit the cells above. Write your own version before opening a hint."}</p>${lesson.assignments.map((a,n)=>`<div class="dl-task"><h3>${i+1}.${n+1} · ${escapeHTML(a.title)}</h3><p>${escapeHTML(a.task)}</p><details><summary>Need a hint?</summary><p>${escapeHTML(a.hint)}</p></details></div>`).join("")}${isTorch ? actions(lesson) : ""}</section>`;
  const runtime = isTorch ? `<aside class="dl-runtime" aria-label="PyTorch runtime instructions"><strong>These examples need PyTorch.</strong><p>Run the cells in order in Colab or local Python—not the standard Room310 compiler. No GPU needed. Each notebook is self-contained.</p>${actions(lesson)}<p class="dl-small"><a href="deep-learning-3-pytorch-and-tensors.html#step-1">First time? Read the setup guide.</a> Colab opens on Google's site; save your own copy to keep edits.</p></aside>` : `<aside class="dl-runtime"><strong>Run these examples right here.</strong><p>Each cell is ready to edit and run with ordinary Python. Prefer a notebook? The downloads include the same lesson and practice tasks.</p>${actions(lesson)}</aside>`;
  const toc = `<aside class="dl-preview-note" hidden><strong>Local preview · use your local notebook</strong><p>To try this local copy, choose <b>Download notebook</b>, then <b>Open Colab</b> and upload the downloaded .ipynb file using File → Upload notebook. On the live site, the Colab button opens the published notebook directly; it does not include unpublished local edits.</p></aside><nav class="dl-toc" aria-label="Lesson sections">${lesson.sections.map((s,n)=>`<a href="#step-${n+1}">${n+1}. ${escapeHTML(s.title)}</a>`).join("")}<a href="#assignments">Practice & explain</a></nav>`;
  const content = `<main class="lesson-page dl-lesson"><header class="lesson-hero dl-lesson-hero"><a href="deep-learning-study.html">← Deep learning course</a><p>Deep learning / Lesson 0${i+1} of 06 / ${lesson.time}</p><h1>${escapeHTML(lesson.title)}</h1><p>${escapeHTML(lesson.description)}</p></header><article class="lesson-content"><div class="dl-goal"><strong>By the end</strong><p>${escapeHTML(lesson.goal)}</p></div>${runtime}${toc}${steps}${assignments}<aside class="dl-takeaway"><strong>The idea to take with you</strong><p>${escapeHTML(lesson.takeaway)}</p></aside><section class="dl-credit"><h2>Keep exploring</h2>${references(lesson.references)}</section></article><nav class="lesson-pagination" aria-label="Deep learning lesson navigation"><a href="${i ? lessons[i-1].slug : "deep-learning-study"}.html"><span>Previous</span>${i ? escapeHTML(lessons[i-1].title) : "Course overview"}</a><a href="${i < lessons.length-1 ? lessons[i+1].slug : "deep-learning-study"}.html"><span>${i < lessons.length-1 ? "Next" : "Course complete"}</span>${i < lessons.length-1 ? escapeHTML(lessons[i+1].title) : "Back to your learning path"} →</a></nav></main>`;
  await writeFile(new URL(`${lesson.slug}.html`, output), shell(lesson.title, lesson.description, "curriculum-page authored-course-page python-page deep-learning-page", content, `${isTorch ? "" : `<script src="python-lab.js?v=${version}"></script>`}<script src="deep-learning.js?v=${version}"></script>`, isTorch ? 'data-runtime="external-pytorch"' : ""));
}
console.log(`Generated deep learning overview, ${lessons.length} lessons, and ${lessons.length} Python downloads.`);
